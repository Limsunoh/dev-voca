# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Django
*.sqlite3
/backend/media/
/backend/staticfiles/

# 환경변수
.env
.env.*
!.env.example

# Node / Next.js
node_modules/
.next/
# D 드라이브가 ACL 미지원이라 .next 가 잠겨 지워지지 않는 일이 있다.
# 그때 distDir 을 임시로 바꿔 빌드하는데, 그 산출물이 커밋되지 않게 막는다.
.next-*/
out/

# 로컬 작업 노트 (커밋 안 함)
.claude/TROUBLESHOOTING.md
.claude/CODE_INSIGHTS.md
.claude/SLASH_COMMANDS_KR.md
.claude/settings.local.json
.claude/verification-state.json

# 브라우저 자동화 임시 산출물
.playwright-mcp/

# 리포지토리에 두면 안 되는 개인 파일
# 작업 폴더에 잠깐 내려받아 두는 일이 있어, git add -A 로 딸려 들어가지 않게 막는다.
recovery-codes.txt
*recovery-code*
*.pdf

# 검증 중 만든 임시 산출물
/backend/probe_*.txt
/backend/apps/**/tests_probe_*.py
/*.png

# 에디터 / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
