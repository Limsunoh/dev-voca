# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Django
*.sqlite3
/backend/media/
/backend/staticfiles/

# 환경변수
.env
.env.*
!.env.example

# Node / Next.js
node_modules/
.next/
# D 드라이브가 ACL 미지원이라 .next 가 잠겨 지워지지 않는 일이 있다.
# 그때 distDir 을 임시로 바꿔 빌드하는데, 그 산출물이 커밋되지 않게 막는다.
.next-*/
out/

# 로컬 작업 노트 (커밋 안 함)
.claude/TROUBLESHOOTING.md
.claude/CODE_INSIGHTS.md
.claude/SLASH_COMMANDS_KR.md
.claude/settings.local.json
.claude/verification-state.json

# 브라우저 자동화 임시 산출물
.playwright-mcp/

# 리포지토리에 두면 안 되는 개인 파일
# 작업 폴더에 잠깐 내려받아 두는 일이 있어, git add -A 로 딸려 들어가지 않게 막는다.
recovery-codes.txt
*recovery-code*
*.pdf

# 다른 세션에서 받은 단어·문장 데이터를 병합 전에 잠깐 두는 자리.
# seed_words.py 에 합치고 나면 지운다 - 두 벌이 남으면 어느 쪽이
# 최신인지 알 수 없게 된다.
/new_words*.py
/review_chunks/

# 검증 중 만든 임시 산출물
/*.log
/backend/probe_*.txt
/backend/apps/**/tests_probe_*.py
/*.png
# 데이터 검수 스크립트가 레포 루트에 뱉는 것들
/inspect*.txt
/dup*.txt
/abbr_check*.txt
/cmp*.txt
/prons*.txt

# 에디터 / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
